# MediaPipe Environment
This virtual environment is located at ~/venvs/cv_mediapipe
Purpose: Real-time facial detection and tracking prototype (MediaPipe test phase).  
Install using: pip install -r cv_model_testing/mediapipe_test/requirements.txt


# DeepFace Environment 
This virtual environment is located at ~/venvs/cv_deepface 
Purpose: Real-time age and gender detection using DeepFace + YOLO integration.
Install using: pip install -r cv_model_testing/deepface/requirements.txt

